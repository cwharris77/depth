import * as parts from '../parts';
import type { UniformPart } from '../../core/parts';

export const JERSEY_ORANGE: UniformPart = parts.jersey(
  'orange',
  'white',
  'navy',
  'white',
  parts.BEARS_MODERN_COLLAR_PATH
);
