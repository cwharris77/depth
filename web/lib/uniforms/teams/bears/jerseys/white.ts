import * as parts from '../parts';
import type { UniformPart } from '../../core/parts';

export const JERSEY_WHITE: UniformPart = parts.jersey(
  'white',
  'navy',
  'orange',
  'navy',
  parts.BEARS_CLASSIC_COLLAR_PATH
);
