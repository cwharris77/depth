source "https://rubygems.org"

# fastlane's `frameit` only — device-frame + caption overlay for the App Store
# screenshots captured by DepthUITests/AppStoreScreenshotsUITests.swift. Not used for
# fastlane's `snapshot`/`deliver`/build lanes; this repo's own XCUITest harness and CI
# already own capture and build, so this stays a single-purpose dependency.
gem "fastlane"
