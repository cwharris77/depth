# Generated by `npm run db:types` — must stay byte-stable with the generator's output,
# or every regeneration produces format-only churn.
lib/database.types.ts

# Checked-in ESPN API response captures — fixtures, not authored code.
lib/espn/fixtures/

# Hand-wrapped prose (README, docs/, specs). Prettier would churn carefully
# authored markdown for no benefit.
*.md

# Supabase CLI-managed files (config.toml, SQL migrations, .temp state).
supabase/

# Build output and lockfile.
.next/
package-lock.json
